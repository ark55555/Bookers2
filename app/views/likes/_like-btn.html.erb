<% if book.liked_by?(current_user) %>
  <%= link_to book.likes.count, book_likes_path(book), method: :delete, remote: true, class: "fas fa-heart heart_red" %>
<% else %>
  <%= link_to book.likes.count, book_likes_path(book), method: :post, remote: true, class: "fas fa-heart heart_blue" %>
<% end %>